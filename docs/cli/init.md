# pantsagon init

Create a new monorepo.

```bash
pantsagon init <repo>
```

Common flags:

- `--lang python` (required in v1)
- `--services a,b`
- `--feature openapi` (repeatable)
- `--feature docker` (repeatable)
- `--strict`
- `--renderer copier`
- `--non-interactive`
