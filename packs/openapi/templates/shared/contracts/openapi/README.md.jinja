# OpenAPI contracts

Specs live here as shared contracts. One or more specs per service is allowed.

The `/health` path in the starter spec is a disposable placeholder.

If an OpenAPI backend is enabled later, these files can be upgraded to specialized targets without changing paths.
