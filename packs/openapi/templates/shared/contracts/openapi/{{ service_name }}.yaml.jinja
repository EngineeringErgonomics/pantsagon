openapi: "3.0.3"
info:
  title: "{{ service_name }} API"
  version: "0.1.0"
paths:
  /health:
    get:
      summary: "Health check (placeholder)"
      responses:
        "200":
          description: "OK"
